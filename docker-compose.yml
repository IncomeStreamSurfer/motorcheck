version: '3.8'

services:
  motorcheck-demo:
    build: .
    container_name: motorcheck-demo
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html:ro
    restart: unless-stopped